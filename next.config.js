
module.exports = {
  reactStrictMode: true,
};

module.exports = {
  i18n: {
    locales: ['en', 'fr'],
    defaultLocale: 'en',
  },
  images: {
    domains: [
      'i.imgur.com',
    ],
  }
};

// const nextTranslate = require('next-translate');

// module.exports = nextTranslate();